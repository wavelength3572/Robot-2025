package frc.robot.subsystems.coral.arm;

import com.revrobotics.spark.config.SparkMaxConfig;

public final class ArmConfigs {
  public static final SparkMaxConfig ArmSubsystem = new SparkMaxConfig();

  static {
    // Configure additional parameters as needed.
    // For example, set idle mode, current limits, etc.
  }

  private ArmConfigs() {}
}
